# timeczar01
This TimeCzar is just about figuring out container in REACT not going to mess with Docker or PHP/MySQL

npm create vite@latest frontend -- --template react

I install react-bootstrap but their navigation is not what I am looking for in regards to the mobile view. I want to have the hamburger menu, the three lines, in the upper left and I want the styled menu icon not just three lines. I also want the menu to pup out from the left. Defintely want to use Bootstrap for everything else.

Will try this node_module because Google said it was popular

install Material UI (MUI) on your React site

npm install @mui/material @emotion/react @emotion/styled

npm install @mui/icons-material

npm install @fontsource/roboto
